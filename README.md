# Ultimate Free Proxy Scrapper & Validator — v2.1 Protected

> Harvest, validate and export free proxies from **18 public sources** — 80-thread validator, auto-save with watermark, **single-file EXE** so nobody can just copy your code.

![Proxy](https://img.shields.io/badge/proxies-18%20sources-6366f1?style=for-the-badge)
![Threads](https://img.shields.io/badge/validator-80%20threads-10b981?style=for-the-badge)
![Protected](https://img.shields.io/badge/protected-HARSHI79--ULTIMATE--PROXY--2026-f43f5e?style=for-the-badge)
![EXE](https://img.shields.io/badge/EXE-Windows%20%7C%20Linux%20%7C%20macOS-0ea5e9?style=for-the-badge)

**© 2026 harshi79 / YorichiiPrime — Watermark: `HARSHI79-ULTIMATE-PROXY-2026`**

A premium dark dashboard + **protected desktop EXE** that scrapes 18 sources in parallel, validates, and auto-saves to `results/YYYY-MM-DD_HH-MM-SS/` with `_AUTHOR.txt` proof — so if someone copies your code, your watermark is still in the title bar, log, and every result file.

<p align="center"><img src="icon.png" width="120" alt="icon"></p>

---

## 📦 What you get (clean, no bloat)

```
main.py              ← Polished Tkinter GUI (splash, tabs, treeview, dark theme) + CLI --auto + --web
proxy_engine.py      ← Shared core: 18 sources, scrape, validate (80 workers), save_results (watermarked)
app.py               ← Flask SSE web dashboard (optional)
templates/index.html ← Premium single-page UI
icon.png / icon.ico  ← App icon
executable/          ← Pre-built single-file distributable (no Python needed after building true EXE)
  UltimateProxyScrapper.pyz  (27 KB, run with: python UltimateProxyScrapper.pyz --auto)
  UltimateProxyScrapper.exe  (same as .pyz, copy for Windows)
  README.txt
build_exe.bat / build_exe.sh ← One-click true Windows EXE builder (PyInstaller, windowed, icon, ~30 MB)
requirements.txt     ← Flask, requests, pysocks, Pillow, pyinstaller
LICENSE              ← MIT with attribution requirement (must keep watermark)
```

**Deleted useless files:** `run_*.bat`, `github_workflow.yml.example`, `UltimateProxyScrapper.spec` (auto-generated) — keeps repo minimal.

---

## 🚀 EXE — Advanced Auto Workflow (protected)

Double-click `executable/UltimateProxyScrapper.exe` (or build true EXE) → **⚡ AUTO** tab → **START AUTO**:

```
[1/3] Scrape 18 sources (ProxyScrape, TheSpeedX, Monosans, RoosterKid, Geonode, JetKai, ClarkTM…) 12 workers → dedupe → shuffle
[2/3] Validate 80 threads via http://httpbin.org/ip → latency (fast<800ms), valid/dead/timeout
[3/3] Save → results/2026-09-04_12-30-45/
```

**Folder after each run (watermarked):**

```
results/
  2026-09-04_12-30-45/
    valid.txt        # only valid, sorted fast→slow
    all.txt / raw_scraped.txt
    valid.json / all.json / valid.csv / all.csv
    http.txt / socks5.txt  # per-protocol
    stats.json       # {total, valid, counts, avg_latency, author, watermark, version}
    _AUTHOR.txt      # ← Proves authenticity: "Generated by harshi79 — HARSHI79-ULTIMATE-PROXY-2026"
  latest/            # copy of last run
```

**Protection:**
- Window title, splash, status bar, log, About dialog all show `© harshi79 — HARSHI79-ULTIMATE-PROXY-2026`.
- `stats.json` + `_AUTHOR.txt` watermark in **every** result.
- Integrity check `verify_integrity()` warns if watermark is removed.
- LICENSE requires attribution — removing it violates license.

### Run the distributable (no build needed)

```bash
# Portable single-file (needs Python 3.10+ installed)
python executable/UltimateProxyScrapper.pyz              # GUI (polished, tabs, splash)
python executable/UltimateProxyScrapper.pyz --auto       # CLI auto
python executable/UltimateProxyScrapper.pyz --auto --limit 1000 --timeout 8 --output results

# Windows (same file renamed to .exe)
python executable/UltimateProxyScrapper.exe --auto --limit 500
```

### Build a true Windows EXE (no Python needed, source hidden)

On **Windows** with Python 3.10+:

```bat
# One double-click:
build_exe.bat
# OR manual:
pip install -r requirements.txt
pyinstaller --onefile --windowed --name UltimateProxyScrapper --icon=icon.ico main.py
# → dist\UltimateProxyScrapper.exe (real PE, ~30 MB, icon, no console, no source)
# Run: dist\UltimateProxyScrapper.exe  or  dist\UltimateProxyScrapper.exe --auto
```

On **Linux/macOS:**
```bash
chmod +x build_exe.sh && ./build_exe.sh
./dist/UltimateProxyScrapper --auto
```

**CLI flags:**

| Flag | Default | Meaning |
|---|---|---|
| `--limit` | `500` | Max scraped (0=no limit) |
| `--timeout` | `8` | Seconds per proxy |
| `--test-url` | `http://httpbin.org/ip` | Test URL |
| `--threads` | `80` | Validate workers |
| `--output` | `results` | Base folder |
| `--sources` | `all` | Comma ids |
| `--protocols` | `http,https,socks4,socks5` | Filter |

---

## 🌐 Web dashboard (optional)

```bash
pip install -r requirements.txt
python app.py  # or python main.py --web --port 5000
# http://localhost:5000 — SSE live progress, pool filters, TXT/JSON/CSV export
```

Deploy: `pip install -r requirements.txt && gunicorn app:app --workers 1 --threads 8 --timeout 180` (Procfile included).

---

## 🔒 Why EXE protects you

- **Code is not visible** in true EXE (PyInstaller bundles to bytecode, plus icon, windowed).
- **Watermark is embedded** in binary title, log, and results — even if someone extracts `.pyz`, your name is still there.
- **LICENSE** legally requires attribution.
- If someone claims they made it, ask them to show `_AUTHOR.txt` — it will still say `harshi79`.

---

## 🔌 Sources (18)

ProxyScrape `http/socks4/socks5` · Proxy-List.download `http/https/socks4` · TheSpeedX `http/socks4/socks5` · Monosans `http/socks4/socks5` · RoosterKid `https/socks4/socks5` · Geonode · JetKai · ClarkTM

Add more in `proxy_engine.py → SOURCES`.

---

## License

**MIT + Attribution** — keep watermark and author in UI and results. See `LICENSE`.  
© 2026 harshi79 / YorichiiPrime — https://github.com/harshi79/Ultimate-Free-Proxy-Scrapper-And-Validator  
Watermark: `HARSHI79-ULTIMATE-PROXY-2026` — do not remove.
